import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  imagePreview: {
    width: '100%',
    height: '100%',
  },
  actionButtons: {
    justifyContent: 'space-between',
    flexDirection: 'row',
    marginRight: 5,
    marginLeft: 5,
  },
});